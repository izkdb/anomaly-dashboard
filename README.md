# Real-Time Anomaly Detection Dashboard

A live streaming dashboard that detects anomalies using online Z-score analysis.

## How to Run
1. `python3 -m venv venv`
2. `source venv/bin/activate`
3. `pip install streamlit numpy pandas plotly`
4. `streamlit run dashboard.py`
